{% extends "base.html.twig" %}
{% block body %}

	<div class = "mainScreen">
		<h1><a href = "{{ path('warehouse') }}"><</a> Užpildyti kliento profilį </h1> <br />
		{% if error is defined %}
			{{error}}
		{% elseif success is defined %}
			{{success}}
		{% endif %}
		<div id="profile">
            {{ form (form) }}
		</div>
	</div>
{% endblock %}